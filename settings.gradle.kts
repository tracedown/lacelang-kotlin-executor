rootProject.name = "lacelang-kt-executor"

// The validator is resolved as a published artifact
// (dev.lacelang:kotlin-validator) from Maven Central, or from the
// local Maven repository (`publishToMavenLocal`) during development.
